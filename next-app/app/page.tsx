
export default function Page() {
  return (
    <main>
      <h1>Inline Copilot — Next.js App</h1>
      <p>This app exposes <code>/api/chat</code> which streams OpenAI Responses API back to the extension.</p>
      <p>Set your <code>OPENAI_API_KEY</code> in <code>.env.local</code> then run <code>npm run dev</code>.</p>
    </main>
  );
}
